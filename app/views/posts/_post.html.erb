<br />
<div class="row-fluid ">
  <div class="offset1 span10 timestamp full-border">
        <%= timeago_tag post.created_at, :nojs => true, :force => true %>
  </div>
</div>
<div class="row-fluid">
  <div class="offset1 span10 full-border">
    <div class="span2 right-border">
      <% if post.user.admin? %>
        <div class="admin"><%= link_to post.user_username, user_path(post.user_id) %></div>
      <% else %>
        <div class="user"><%= link_to post.user_username, user_path(post.user_id) %></div>
      <% end %>

      <p>
        Joined: <br /><%= timeago_tag post.user_created_at, :nojs => true, :force => true %>
      </p>
    </div>
    <div class="span8">
      <% if can? :destroy, post %>
        <%= link_to 'Delete Post', [post.topic, post],
                 :confirm => 'Are you sure?',
                 :method => :delete,
                 :class => 'btn btn-small btn-danger float-right margin-top' %>
       <% end %>
      <p class="content">
        <%= post.body %>
      </p>
    </div>
  </div>
</div>